<ui:UiWindow x:Class="FluentNgo.Views.Components.StudentForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FluentNgo.Views.Components"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        WindowStyle="None" ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        AllowsTransparency="True"
        WindowBackdropType="Acrylic"
        WindowStartupLocation="CenterOwner"
        Title="StudentForm"
        SizeToContent="Width"
        Height="520">
    
    <Grid x:Name="RootGrid" Focusable="True">

        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="64"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Student"
                   FontSize="32" Padding="16"/>

        <TextBox
            materialDesign:HintAssist.Hint="Test"
            Style="{StaticResource MaterialDesignFloatingHintTextBox}"
            VerticalAlignment="Center" />
    </Grid>
</ui:UiWindow>
